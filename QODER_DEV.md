# Qoder Dev - 适配 QoderWork 的自动开发流程

> 这是 adev 的 QoderWork 适配版本，让我在开发过程中遵循结构化流程。

## 与原版 adev 的区别

| 原版 adev (Claude Code) | Qoder Dev (QoderWork) |
|------------------------|----------------------|
| 需要 `init.sh` 启动环境 | 环境已就绪，跳过初始化 |
| AI 自主循环执行 | 用户驱动，确认后执行 |
| 需要 Playwright MCP | 内置 browser skill |
| 6 步强制流程 | 4 步简化流程 |
| 跨会话传递上下文 | 同一会话内连续执行 |

## 我的工作流程

当你说 **"开始开发模式"** 或 **"/dev:next"** 时，我会：

### Step 1: 读取任务
- 读取 `task.json`
- 显示当前所有任务状态
- 建议下一个要执行的任务

### Step 2: 确认任务
- 向你确认任务理解是否正确
- 如有需要，调整或细化任务

### Step 3: 执行任务
- 按照 task steps 逐步实现
- 使用我的 skills（browser、docx、pdf 等）辅助开发
- 中间过程保持透明，关键步骤向你汇报

### Step 4: 验证与记录
- 运行测试/验证（lint、build、浏览器测试等）
- 更新 `task.json`（标记完成）
- 更新 `progress.txt`（记录工作内容和注意事项）
- 生成变更摘要

## 文件结构

```
project/
├── QODER_DEV.md          # 本文件 - 工作流程说明
├── task.json             # 任务清单
├── progress.txt          # 工作日志
└── .qoder/               # QoderWork 特定配置
    └── commands/         # 快捷指令
        ├── next.md       # /dev:next - 执行下一个任务
        ├── status.md     # /dev:status - 查看进度
        ├── add-task.md   # /dev:add - 添加任务
        └── refactor.md   # /dev:refactor - 重构建议
```

## 快捷指令

在对话中直接输入：

| 指令 | 作用 |
|------|------|
| `/dev:next` | 读取 task.json，执行下一个未完成任务 |
| `/dev:status` | 显示项目进度摘要 |
| `/dev:add` | 添加新任务到 task.json |
| `/dev:refactor` | 分析代码，提出重构建议 |
| `/dev:pause` | 暂停开发模式，记录当前状态 |
| `/dev:resume` | 恢复之前的开发状态 |

## task.json 格式

```json
{
  "project": "项目名称",
  "currentSession": {
    "startedAt": "2026-03-09",
    "tasksCompleted": 0,
    "lastTaskId": null
  },
  "tasks": [
    {
      "id": 1,
      "title": "任务标题",
      "description": "任务描述",
      "steps": [
        "具体步骤 1",
        "具体步骤 2",
        "具体步骤 3"
      ],
      "status": "pending",
      "priority": "high",
      "estimatedTime": "30min",
      "completedAt": null,
      "notes": ""
    }
  ]
}
```

## progress.txt 格式

```markdown
# 项目开发日志

## Session 2026-03-09

### Task 1: [任务标题] ✅
**时间**: 15:00 - 15:45
**完成内容**:
- [具体做了什么]

**测试结果**:
- [测试情况]

**遇到的问题**:
- [问题及解决方案]

**下一步注意**:
- [给后续开发的提示]

---

### Task 2: [任务标题] 🔄
**状态**: 进行中
**已完成**:
- [已完成的部分]

**待完成**:
- [剩余工作]

**阻塞**:
- [是否需要用户决策]
```

## 开发模式状态机

```
[空闲] --/dev:next--> [读取任务] --确认--> [执行中] --完成--> [验证记录] --提交--> [空闲]
                              ^                                    |
                              |_____________调整___________________|
```

## 我的承诺

在开发模式下，我会：

1. **透明沟通** - 每个关键步骤向你汇报，不擅自做重大决策
2. **一次一个任务** - 完成当前任务后再建议下一个
3. **完整记录** - 所有工作记录到 progress.txt，便于追溯
4. **质量验证** - 不标记任务完成直到通过测试
5. **保持专注** - 不偏离当前任务范围（除非发现阻塞问题）

## 使用示例

**场景 1: 开始新项目**

```
你: 帮我创建一个博客系统，使用 Next.js + Prisma
我: [生成 task.json，拆解为多个任务]
你: /dev:next
我: [执行任务 1: 项目初始化]
...
```

**场景 2: 继续开发**

```
你: /dev:next
我: [读取 progress.txt，了解上下文]
我: 上一个任务已完成。下一个任务是 "实现用户认证"，包括：
   1. 创建登录页面
   2. 实现 JWT 验证
   3. 添加中间件保护路由
   预计 30 分钟，是否开始？
你: 开始
我: [执行任务...]
```

**场景 3: 查看状态**

```
你: /dev:status
我: 
   项目: 博客系统
   总任务: 8 个
   已完成: 3 个
   进行中: 1 个 (用户认证)
   待开始: 4 个
   
   最近完成:
   - Task 3: 数据库 Schema 设计 (15:00)
   
   当前进行:
   - Task 4: 用户认证 (步骤 2/3)
```

## 初始化命令

让我为当前项目初始化 Qoder Dev：
